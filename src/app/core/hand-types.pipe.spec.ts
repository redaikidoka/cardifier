import { HandTypesPipe } from './hand-types.pipe';

describe('HandTypesPipe', () => {
  it('create an instance', () => {
    const pipe = new HandTypesPipe();
    expect(pipe).toBeTruthy();
  });
});
